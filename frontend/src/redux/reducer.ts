import { Reducer } from "redux";

// const INITIAL_STATE = {

// };

export const reducer: Reducer = (state: {}, action: { type: string }) => {
  switch (action.type) {
    default: return state;
  }
}
